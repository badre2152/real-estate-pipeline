services:

  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  postgres:
    image: postgres:15
    container_name: avito_postgres3
    environment:
      POSTGRES_USER:     ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB:       ${DB_NAME}
      
    ports:
      
      
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      
      test: ["CMD-SHELL", "PGPASSWORD=${DB_PASSWORD} psql -U ${DB_USER} -d ${DB_NAME} -c 'SELECT 1' -q --no-align --tuples-only"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Pipeline ────────────────────────────────────────────────────────────────
  pipeline:
    build: .
    container_name: avito_pipeline3
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    # Chromium needs sufficient /dev/shm for 25 pages — default 64MB causes crash
    shm_size: "512mb"
    # Prevent infinite restart if scraper fails — max 3 attempts
    restart: on-failure:3
    command: python src/pipeline.py

volumes:
  postgres_data: